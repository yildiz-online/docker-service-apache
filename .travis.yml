sudo: required
services:
  - docker

before_install:
  - docker build -t moussavdb/web-server-image:latest -t moussavdb/web-server-image:build_$TRAVIS_BUILD_NUMBER .
script:
  - docker images moussavdb/nodejs
  
after_success:
  - docker login -u="moussavdb" -p="$DOCKER_PASSWORD";
    docker push moussavdb/web-server-image:latest;
    docker push moussavdb/web-server-image:build_$TRAVIS_BUILD_NUMBER;
